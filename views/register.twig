{% extends "template/template.twig" %}
{% block styles %}
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css">
<link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/v/bs4/dt-1.10.16/datatables.min.css"/>
{% endblock %}
{% block content %}
<div class="ed-container full">
    <div class="ed-item s-100 l-offset-15 l-70">
        <h2 class="text-left text-info">Módulo matriculas</h2>
    </div>
    <div class="ed-item s-100">
        <button class="btn btn-primary addregister" data-href="{{ path_for('admin.store.register') }}"><i class="icon icon-plus-circle"></i> Agregar matricula</button>  -  <a class="btn btn-success" href="{{ path_for('admin.view.user.upload') }}"> <i class="icon icon-file-text-o"></i> Cargar archivo</a>
        <br>
    </div>
    <div class="ed-item s-100">
        <table id="tb_register" class="display">
            <thead>
                <tr>
                    <th>A:</th>
                    <th> {{ "curso" | title }}</th>
                    <th> {{ "instancia" | title }}</th>
                    <th> {{ "usuario" | title }}</th>
                    <th> {{ "rol" | title }}</th>
                    <th> {{ "fecha" | title }}</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>

</div>
{% endblock %}

{% block modales %}
    {% include "formularios/register.twig" %}
    {% include "formularios/student.twig" %}
{% endblock %}

{% block scripts %}
<script type="text/javascript" src="https://cdn.datatables.net/v/bs4/dt-1.10.16/datatables.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/2.1.4/toastr.min.js"></script>
<script>
var getUri = "http://"+window.location.host;
var _td = null;
var table = $('table').DataTable({
    language: {
        "lengthMenu": "Mostrar _MENU_ registros por pagina",
        "zeroRecords": "No hay resultados",
        "info": "Pagina _PAGE_ de _PAGES_",
        "infoEmpty": "No registros disponible",
        "infoFiltered": "(Busqueda de _MAX_ total registros)",
        "processing": "Procesando...",
        "search": "Buscar:",
        "loadingRecords": "Cargando...",
        "next" : "Siguiente",
        "paginate": {
            "first":      "Primera",
            "last":       "Ultima",
            "next":       "Siguiente",
            "previous":   "Anterior"
        },
    },
    ajax: {
        url: getUri + '/panel/register/all',
        dataSrc: ''
    },
    columns: [
        { data: 'id' },
        { data: 'curso' },
        { data: 'instancia' },
        { data: 'usuario' },
        { data: 'rol' },
        { data: 'fecha' },
        { data: null,
          defaultContent:"<a href='{{ path_for('admin.delete.register', {id : id}) }}' class='registertEliminar pointer'><i class='icon icon-trash text-danger'></i></a><a href='{{ path_for('admin.show.register', {id : data.id}) }}' class='registerShow pointer' data-href='{{ path_for('admin.update.register', {id : data.id}) }}'><i class='icon icon-user text-sucess'></i></a>"
        }
      ]
});
</script>
<script src="{{ base_url }}/js/modules/register.js"></script>
{% endblock %}