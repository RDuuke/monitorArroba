<table>
    <thead>
    <tr>
        <th>Curso</th>
        <th>Código</th>
        <th>Fecha</th>
        <th>Total</th>
    </tr>
    </thead>
    <tbody>
    {% for c in courses %}
        <tr>
            <td>{{ c.nombre }}</td>
            <td>{{ c.codigo }}</td>
            <td>{{ c.fecha }}</td>
            <td>{{ c.registers.count }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>