{% extends "template/template.twig" %}
{% block styles %}
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css">
<link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/v/bs4/dt-1.10.16/datatables.min.css"/>
<style>
  @media (min-width: 992px){
    .modal-lg {
      max-width: 1075px;
    }
  }
</style>
{% endblock %}
{% block content %}
<div class="ed-container full">
    <div class="ed-item s-100 l-offset-15 l-70">
        <h2 class="text-left text-info">Usuarios campus</h2>
    </div>
    <div class="ed-item s-100">
        <a href="{{ path_for('admin.home') }}" class="btn btn-info"> <i class="fa fa-long-arrow-left"></i> Volver</a>
    </div>
    <div class="separator"></div>
    {% if getPermission(modulo_campus, auth.user.id) == 3 %}
    <div class="ed-item s-100">
        <button class="btn btn-primary addstudent" data-href="{{ path_for('admin.students.store') }}"><i class="icon icon-plus-circle"></i> Agregar usuario</button>  -  <a class="btn btn-success" href="{{ path_for('admin.upload.students') }}"> <i class="icon icon-file-text-o"></i> Cargar archivo</a>
        <br>
    </div>
    {% endif %}
    <div class="ed-item s-100">
    <br>
        <table id="tb_user" class="display">
            <thead>
                <tr>
                    <th>ID:</th>
                    <th> {{ "Usuario" | title }}</th>
                    <th> {{ "nombres" | title }}</th>
                    <th> {{ "Apellidos" | title }}</th>
                    <th> {{ "Documento" | title }}</th>
                    <th> {{ "Institucion" | title }}</th>
                    <th> {{ 'Acciones' | title }}</th>
                    <!--
                    <th> {{ "Genero" | title }}</th>
                    <th> {{ "Ciudad" | title }}</th>
                    <th> {{ "Departamento" | title }}</th>
                    <th> {{ "Pais" | title }}</th>
                    <th> {{ "Telefono" | title }}</th>
                    <th> {{ "Celular" | title }}</th>
                    <th> {{ "Direccion" | title }}</th>-->
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
{% block modales %}
{% include "formularios/student.twig" %}
{% include "formularios/register_student.twig" %}
{% endblock %}
{% block scripts %}
<script type="text/javascript" src="https://cdn.datatables.net/v/bs4/dt-1.10.16/datatables.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/2.1.4/toastr.min.js"></script>
<script>
var _td = null;
var table = $('table#tb_user').DataTable({
        lengthMenu: [ 25, 50, 75, 100 ],
        language: {
            "lengthMenu": "Mostrar _MENU_ registros por pagina",
            "zeroRecords": "No hay resultados",
            "info": "Pagina _PAGE_ de _PAGES_",
            "infoEmpty": "No registros disponible",
            "infoFiltered": "(Busqueda de _MAX_ total registros)",
            "processing": "Procesando...",
            "search": "Buscar:",
            "loadingRecords": "Cargando...",
            "next" : "Siguiente",
            "paginate": {
                "first":      "Primera",
                "last":       "Ultima",
                "next":       "Siguiente",
                "previous":   "Anterior"
            },
        },
        ajax: {
            url: getUri + '/panel/students/all',
            dataSrc: ''
        },
        columns: [
            { data: 'id' },
            { data: 'usuario' },
            { data: 'nombres' },
            { data: 'apellidos' },
            { data: 'documento' },
            { data: 'institucion' },
            {% if getPermission(modulo_campus, auth.user.id) == 3 %}
            { data: null,
              defaultContent:"<a data-toggle='tooltip' title='Matriculas' href='{{ path_for('admin.search.program.course.student.data', {id : id}) }}' class='green icon_a pointer searchRegister' ><i class='fa fa-search'></i></a>&nbsp;<a data-toggle='tooltip' title='Eliminar' href='{{ path_for('admin.students.delete', {id : id}) }}' class='studentEliminar pointer icon_a'><i class='fa fa-trash text-danger'></i></a>&nbsp;<a data-toggle='tooltip' title='Actualizar' href='{{ path_for('admin.students.show', {id : data.id}) }}' class='studentShow pointer icon_a' data-href='{{ path_for('admin.students.update', {id : data.id}) }}'><i class='fa fa-pencil text-sucess'></i></a>"
            }
            {% else %}
            { data: null,
              defaultContent:"<a data-toggle='tooltip' title='Cursos' href='{{ path_for('admin.search.program.course.student.data', {id : id}) }}' class='green icon_a pointer searchRegister' ><i class='fa fa-search'></i></a>&nbsp<a data-toggle='tooltip' title='Información' href='{{ path_for('admin.students.show', {id : data.id}) }}' class='studentShow pointer icon_a' data-href='{{ path_for('admin.students.update', {id : data.id}) }}'><i class='fa fa-pencil text-sucess'></i></a>"
            }
            {% endif %}
          ]
    });

    var table_register = $('table#result_student_register_table').DataTable({
        lengthMenu: [ 25, 50, 75, 100 ],
        language: {
            "lengthMenu": "Mostrar _MENU_ registros por pagina",
            "zeroRecords": "No hay resultados",
            "info": "Pagina _PAGE_ de _PAGES_",
            "infoEmpty": "No registros disponible",
            "infoFiltered": "(Busqueda de _MAX_ total registros)",
            "processing": "Procesando...",
            "search": "Buscar:",
            "loadingRecords": "Cargando...",
            "next" : "Siguiente",
            "paginate": {
                "first":      "Primera",
                "last":       "Ultima",
                "next":       "Siguiente",
                "previous":   "Anterior"
            },
        }
     });
</script>
</script>
<script src="{{ base_url }}/monitor/public/js/function.js"></script>
<script src="{{ base_url }}/monitor/public/js/modules/student.js"></script>
{% endblock %}
